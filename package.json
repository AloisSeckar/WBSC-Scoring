{
  "name": "wbsc-scoring",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "eslint": "eslint .",
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "node .output/server/index.mjs",
    "test": "pnpm test-validations && pnpm test-actions",
    "test-validations": "vitest run --project validations",
    "test-actions": "vitest run --project actions"
  },
  "devDependencies": {
    "nuxt-ignis": "0.6.0"
  },
  "packageManager": "pnpm@11.9.0"
}
